.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install lifter

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv lifter
    $ pip install lifter
